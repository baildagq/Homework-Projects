debug protocols all;

protocol kernel {
	persist;                # Don't remove routes on BIRD shutdown
	learn;
	export all;     # Default is export none
}

protocol device {
}

protocol static {
    route 192.168.1.0/24 via "veth-1l";
    route 192.168.3.0/24 via "veth-1r";
}

protocol rip {
	import all;
	export all;
	interface "*" {
		version 2;
		update time 30;
	};
}
